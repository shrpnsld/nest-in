open-file()
{
	local descriptor=$1
	local path=$2

	eval "exec $descriptor< '$path'" > /dev/null 2>&1
	if [[ $? != 0 ]]
	then
		return 1
	fi
}

close-file()
{
	local descriptor=$1

	eval "exec $descriptor>&-"
}
